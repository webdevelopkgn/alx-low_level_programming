#include "main.h"

/**
 * _isupper - checks if there is an uppercase character
 * 
 * Return: 1 means that c is uppercase, 0 it is not
 */
int _isupper(int letter)
{
	return (letter >= 'A' && letter <= 'Z');
}
